# Tahoe-100M public UI — pinned to versions verified on Argon (CentOS 7,
# glibc 2.17, Python 3.11). pyarrow is held at <17 because pyarrow>=17 wheels
# require glibc 2.28 and will not install on CentOS 7. These pins also install
# cleanly on Streamlit Community Cloud.
huggingface_hub==1.28.0
pyarrow==16.1.0
duckdb==1.2.2
polars==1.44.0
pandas==2.3.2
scipy==1.16.3
statsmodels==0.14.6
plotly==7.0.0
streamlit==1.62.0
pytest==9.1.1
